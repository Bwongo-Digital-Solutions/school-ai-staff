node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Generated native projects (expo prebuild)
# The native project, generated by `expo prebuild`. Anchored to the repository root: an
# unanchored `android/` matches any directory of that name at any depth, which silently
# swallowed the whole of fastlane/metadata/android/ — the store listing.
/android/
ios/

# Build output
*.apk
*.aab
